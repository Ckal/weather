name: weather
type: gradio-space
sdk: gradio

metadata:
  space_id: Chris4K/weather
  original_url: https://huggingface.co/spaces/Chris4K/weather

deploy:
  targets:
    - public-repo
    - huggingface

build:
  command: bash build.sh

test:
  command: echo "Tests not yet implemented"

# Updated: 2026-01-03 12:59:58
